name: toxcom
channels:
  - conda-forge
  - defaults
  - anaconda
dependencies:
  # Core stack
  - python>=3.6
  - nltk
  - numpy
  - scipy
  - matplotlib
  - pandas
  - pytorch-gpu
  - keras-gpu
  - scikit-learn
  - requests
  - pip
  - pip:
    - regional
    - transformers
    - bert-tensorflow
    - tensorboardX

  # Testing
  - pytest